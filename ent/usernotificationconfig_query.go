// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/anzhiyu-c/anheyu-app/ent/notificationtype"
	"github.com/anzhiyu-c/anheyu-app/ent/predicate"
	"github.com/anzhiyu-c/anheyu-app/ent/user"
	"github.com/anzhiyu-c/anheyu-app/ent/usernotificationconfig"
)

// UserNotificationConfigQuery is the builder for querying UserNotificationConfig entities.
type UserNotificationConfigQuery struct {
	config
	ctx                  *QueryContext
	order                []usernotificationconfig.OrderOption
	inters               []Interceptor
	predicates           []predicate.UserNotificationConfig
	withUser             *UserQuery
	withNotificationType *NotificationTypeQuery
	modifiers            []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserNotificationConfigQuery builder.
func (uncq *UserNotificationConfigQuery) Where(ps ...predicate.UserNotificationConfig) *UserNotificationConfigQuery {
	uncq.predicates = append(uncq.predicates, ps...)
	return uncq
}

// Limit the number of records to be returned by this query.
func (uncq *UserNotificationConfigQuery) Limit(limit int) *UserNotificationConfigQuery {
	uncq.ctx.Limit = &limit
	return uncq
}

// Offset to start from.
func (uncq *UserNotificationConfigQuery) Offset(offset int) *UserNotificationConfigQuery {
	uncq.ctx.Offset = &offset
	return uncq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uncq *UserNotificationConfigQuery) Unique(unique bool) *UserNotificationConfigQuery {
	uncq.ctx.Unique = &unique
	return uncq
}

// Order specifies how the records should be ordered.
func (uncq *UserNotificationConfigQuery) Order(o ...usernotificationconfig.OrderOption) *UserNotificationConfigQuery {
	uncq.order = append(uncq.order, o...)
	return uncq
}

// QueryUser chains the current query on the "user" edge.
func (uncq *UserNotificationConfigQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: uncq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := uncq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := uncq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usernotificationconfig.Table, usernotificationconfig.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, usernotificationconfig.UserTable, usernotificationconfig.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(uncq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNotificationType chains the current query on the "notification_type" edge.
func (uncq *UserNotificationConfigQuery) QueryNotificationType() *NotificationTypeQuery {
	query := (&NotificationTypeClient{config: uncq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := uncq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := uncq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usernotificationconfig.Table, usernotificationconfig.FieldID, selector),
			sqlgraph.To(notificationtype.Table, notificationtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, usernotificationconfig.NotificationTypeTable, usernotificationconfig.NotificationTypeColumn),
		)
		fromU = sqlgraph.SetNeighbors(uncq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserNotificationConfig entity from the query.
// Returns a *NotFoundError when no UserNotificationConfig was found.
func (uncq *UserNotificationConfigQuery) First(ctx context.Context) (*UserNotificationConfig, error) {
	nodes, err := uncq.Limit(1).All(setContextOp(ctx, uncq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usernotificationconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) FirstX(ctx context.Context) *UserNotificationConfig {
	node, err := uncq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserNotificationConfig ID from the query.
// Returns a *NotFoundError when no UserNotificationConfig ID was found.
func (uncq *UserNotificationConfigQuery) FirstID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = uncq.Limit(1).IDs(setContextOp(ctx, uncq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usernotificationconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) FirstIDX(ctx context.Context) uint {
	id, err := uncq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserNotificationConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserNotificationConfig entity is found.
// Returns a *NotFoundError when no UserNotificationConfig entities are found.
func (uncq *UserNotificationConfigQuery) Only(ctx context.Context) (*UserNotificationConfig, error) {
	nodes, err := uncq.Limit(2).All(setContextOp(ctx, uncq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usernotificationconfig.Label}
	default:
		return nil, &NotSingularError{usernotificationconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) OnlyX(ctx context.Context) *UserNotificationConfig {
	node, err := uncq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserNotificationConfig ID in the query.
// Returns a *NotSingularError when more than one UserNotificationConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (uncq *UserNotificationConfigQuery) OnlyID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = uncq.Limit(2).IDs(setContextOp(ctx, uncq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usernotificationconfig.Label}
	default:
		err = &NotSingularError{usernotificationconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) OnlyIDX(ctx context.Context) uint {
	id, err := uncq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserNotificationConfigs.
func (uncq *UserNotificationConfigQuery) All(ctx context.Context) ([]*UserNotificationConfig, error) {
	ctx = setContextOp(ctx, uncq.ctx, ent.OpQueryAll)
	if err := uncq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserNotificationConfig, *UserNotificationConfigQuery]()
	return withInterceptors[[]*UserNotificationConfig](ctx, uncq, qr, uncq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) AllX(ctx context.Context) []*UserNotificationConfig {
	nodes, err := uncq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserNotificationConfig IDs.
func (uncq *UserNotificationConfigQuery) IDs(ctx context.Context) (ids []uint, err error) {
	if uncq.ctx.Unique == nil && uncq.path != nil {
		uncq.Unique(true)
	}
	ctx = setContextOp(ctx, uncq.ctx, ent.OpQueryIDs)
	if err = uncq.Select(usernotificationconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) IDsX(ctx context.Context) []uint {
	ids, err := uncq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uncq *UserNotificationConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uncq.ctx, ent.OpQueryCount)
	if err := uncq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uncq, querierCount[*UserNotificationConfigQuery](), uncq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) CountX(ctx context.Context) int {
	count, err := uncq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uncq *UserNotificationConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uncq.ctx, ent.OpQueryExist)
	switch _, err := uncq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uncq *UserNotificationConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := uncq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserNotificationConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uncq *UserNotificationConfigQuery) Clone() *UserNotificationConfigQuery {
	if uncq == nil {
		return nil
	}
	return &UserNotificationConfigQuery{
		config:               uncq.config,
		ctx:                  uncq.ctx.Clone(),
		order:                append([]usernotificationconfig.OrderOption{}, uncq.order...),
		inters:               append([]Interceptor{}, uncq.inters...),
		predicates:           append([]predicate.UserNotificationConfig{}, uncq.predicates...),
		withUser:             uncq.withUser.Clone(),
		withNotificationType: uncq.withNotificationType.Clone(),
		// clone intermediate query.
		sql:       uncq.sql.Clone(),
		path:      uncq.path,
		modifiers: append([]func(*sql.Selector){}, uncq.modifiers...),
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (uncq *UserNotificationConfigQuery) WithUser(opts ...func(*UserQuery)) *UserNotificationConfigQuery {
	query := (&UserClient{config: uncq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	uncq.withUser = query
	return uncq
}

// WithNotificationType tells the query-builder to eager-load the nodes that are connected to
// the "notification_type" edge. The optional arguments are used to configure the query builder of the edge.
func (uncq *UserNotificationConfigQuery) WithNotificationType(opts ...func(*NotificationTypeQuery)) *UserNotificationConfigQuery {
	query := (&NotificationTypeClient{config: uncq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	uncq.withNotificationType = query
	return uncq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserNotificationConfig.Query().
//		GroupBy(usernotificationconfig.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uncq *UserNotificationConfigQuery) GroupBy(field string, fields ...string) *UserNotificationConfigGroupBy {
	uncq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserNotificationConfigGroupBy{build: uncq}
	grbuild.flds = &uncq.ctx.Fields
	grbuild.label = usernotificationconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserNotificationConfig.Query().
//		Select(usernotificationconfig.FieldCreatedAt).
//		Scan(ctx, &v)
func (uncq *UserNotificationConfigQuery) Select(fields ...string) *UserNotificationConfigSelect {
	uncq.ctx.Fields = append(uncq.ctx.Fields, fields...)
	sbuild := &UserNotificationConfigSelect{UserNotificationConfigQuery: uncq}
	sbuild.label = usernotificationconfig.Label
	sbuild.flds, sbuild.scan = &uncq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserNotificationConfigSelect configured with the given aggregations.
func (uncq *UserNotificationConfigQuery) Aggregate(fns ...AggregateFunc) *UserNotificationConfigSelect {
	return uncq.Select().Aggregate(fns...)
}

func (uncq *UserNotificationConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uncq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uncq); err != nil {
				return err
			}
		}
	}
	for _, f := range uncq.ctx.Fields {
		if !usernotificationconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uncq.path != nil {
		prev, err := uncq.path(ctx)
		if err != nil {
			return err
		}
		uncq.sql = prev
	}
	return nil
}

func (uncq *UserNotificationConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserNotificationConfig, error) {
	var (
		nodes       = []*UserNotificationConfig{}
		_spec       = uncq.querySpec()
		loadedTypes = [2]bool{
			uncq.withUser != nil,
			uncq.withNotificationType != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserNotificationConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserNotificationConfig{config: uncq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(uncq.modifiers) > 0 {
		_spec.Modifiers = uncq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uncq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := uncq.withUser; query != nil {
		if err := uncq.loadUser(ctx, query, nodes, nil,
			func(n *UserNotificationConfig, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := uncq.withNotificationType; query != nil {
		if err := uncq.loadNotificationType(ctx, query, nodes, nil,
			func(n *UserNotificationConfig, e *NotificationType) { n.Edges.NotificationType = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (uncq *UserNotificationConfigQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserNotificationConfig, init func(*UserNotificationConfig), assign func(*UserNotificationConfig, *User)) error {
	ids := make([]uint, 0, len(nodes))
	nodeids := make(map[uint][]*UserNotificationConfig)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (uncq *UserNotificationConfigQuery) loadNotificationType(ctx context.Context, query *NotificationTypeQuery, nodes []*UserNotificationConfig, init func(*UserNotificationConfig), assign func(*UserNotificationConfig, *NotificationType)) error {
	ids := make([]uint, 0, len(nodes))
	nodeids := make(map[uint][]*UserNotificationConfig)
	for i := range nodes {
		fk := nodes[i].NotificationTypeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(notificationtype.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "notification_type_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (uncq *UserNotificationConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uncq.querySpec()
	if len(uncq.modifiers) > 0 {
		_spec.Modifiers = uncq.modifiers
	}
	_spec.Node.Columns = uncq.ctx.Fields
	if len(uncq.ctx.Fields) > 0 {
		_spec.Unique = uncq.ctx.Unique != nil && *uncq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uncq.driver, _spec)
}

func (uncq *UserNotificationConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usernotificationconfig.Table, usernotificationconfig.Columns, sqlgraph.NewFieldSpec(usernotificationconfig.FieldID, field.TypeUint))
	_spec.From = uncq.sql
	if unique := uncq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uncq.path != nil {
		_spec.Unique = true
	}
	if fields := uncq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usernotificationconfig.FieldID)
		for i := range fields {
			if fields[i] != usernotificationconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if uncq.withUser != nil {
			_spec.Node.AddColumnOnce(usernotificationconfig.FieldUserID)
		}
		if uncq.withNotificationType != nil {
			_spec.Node.AddColumnOnce(usernotificationconfig.FieldNotificationTypeID)
		}
	}
	if ps := uncq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uncq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uncq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uncq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uncq *UserNotificationConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uncq.driver.Dialect())
	t1 := builder.Table(usernotificationconfig.Table)
	columns := uncq.ctx.Fields
	if len(columns) == 0 {
		columns = usernotificationconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uncq.sql != nil {
		selector = uncq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uncq.ctx.Unique != nil && *uncq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range uncq.modifiers {
		m(selector)
	}
	for _, p := range uncq.predicates {
		p(selector)
	}
	for _, p := range uncq.order {
		p(selector)
	}
	if offset := uncq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uncq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uncq *UserNotificationConfigQuery) Modify(modifiers ...func(s *sql.Selector)) *UserNotificationConfigSelect {
	uncq.modifiers = append(uncq.modifiers, modifiers...)
	return uncq.Select()
}

// UserNotificationConfigGroupBy is the group-by builder for UserNotificationConfig entities.
type UserNotificationConfigGroupBy struct {
	selector
	build *UserNotificationConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uncgb *UserNotificationConfigGroupBy) Aggregate(fns ...AggregateFunc) *UserNotificationConfigGroupBy {
	uncgb.fns = append(uncgb.fns, fns...)
	return uncgb
}

// Scan applies the selector query and scans the result into the given value.
func (uncgb *UserNotificationConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uncgb.build.ctx, ent.OpQueryGroupBy)
	if err := uncgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserNotificationConfigQuery, *UserNotificationConfigGroupBy](ctx, uncgb.build, uncgb, uncgb.build.inters, v)
}

func (uncgb *UserNotificationConfigGroupBy) sqlScan(ctx context.Context, root *UserNotificationConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uncgb.fns))
	for _, fn := range uncgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uncgb.flds)+len(uncgb.fns))
		for _, f := range *uncgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uncgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uncgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserNotificationConfigSelect is the builder for selecting fields of UserNotificationConfig entities.
type UserNotificationConfigSelect struct {
	*UserNotificationConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (uncs *UserNotificationConfigSelect) Aggregate(fns ...AggregateFunc) *UserNotificationConfigSelect {
	uncs.fns = append(uncs.fns, fns...)
	return uncs
}

// Scan applies the selector query and scans the result into the given value.
func (uncs *UserNotificationConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uncs.ctx, ent.OpQuerySelect)
	if err := uncs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserNotificationConfigQuery, *UserNotificationConfigSelect](ctx, uncs.UserNotificationConfigQuery, uncs, uncs.inters, v)
}

func (uncs *UserNotificationConfigSelect) sqlScan(ctx context.Context, root *UserNotificationConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(uncs.fns))
	for _, fn := range uncs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*uncs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uncs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (uncs *UserNotificationConfigSelect) Modify(modifiers ...func(s *sql.Selector)) *UserNotificationConfigSelect {
	uncs.modifiers = append(uncs.modifiers, modifiers...)
	return uncs
}
