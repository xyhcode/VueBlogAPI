// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/anzhiyu-c/anheyu-app/ent/fcirclepost"
)

// 朋友圈文章表
type FCirclePost struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 文章标题
	Title string `json:"title,omitempty"`
	// 文章链接
	Link string `json:"link,omitempty"`
	// 创建时间
	Created time.Time `json:"created,omitempty"`
	// 更新时间
	Updated time.Time `json:"updated,omitempty"`
	// 作者名称
	Author string `json:"author,omitempty"`
	// 作者头像链接
	Avatar string `json:"avatar,omitempty"`
	// 友链用户网站链接
	FriendLink string `json:"friend_link,omitempty"`
	// 爬取时间
	CrawledAt time.Time `json:"crawled_at,omitempty"`
	// 使用的规则类型
	Rules        string `json:"rules,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FCirclePost) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case fcirclepost.FieldID:
			values[i] = new(sql.NullInt64)
		case fcirclepost.FieldTitle, fcirclepost.FieldLink, fcirclepost.FieldAuthor, fcirclepost.FieldAvatar, fcirclepost.FieldFriendLink, fcirclepost.FieldRules:
			values[i] = new(sql.NullString)
		case fcirclepost.FieldCreated, fcirclepost.FieldUpdated, fcirclepost.FieldCrawledAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FCirclePost fields.
func (fp *FCirclePost) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case fcirclepost.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			fp.ID = int(value.Int64)
		case fcirclepost.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				fp.Title = value.String
			}
		case fcirclepost.FieldLink:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field link", values[i])
			} else if value.Valid {
				fp.Link = value.String
			}
		case fcirclepost.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				fp.Created = value.Time
			}
		case fcirclepost.FieldUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated", values[i])
			} else if value.Valid {
				fp.Updated = value.Time
			}
		case fcirclepost.FieldAuthor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field author", values[i])
			} else if value.Valid {
				fp.Author = value.String
			}
		case fcirclepost.FieldAvatar:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field avatar", values[i])
			} else if value.Valid {
				fp.Avatar = value.String
			}
		case fcirclepost.FieldFriendLink:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field friend_link", values[i])
			} else if value.Valid {
				fp.FriendLink = value.String
			}
		case fcirclepost.FieldCrawledAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field crawled_at", values[i])
			} else if value.Valid {
				fp.CrawledAt = value.Time
			}
		case fcirclepost.FieldRules:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rules", values[i])
			} else if value.Valid {
				fp.Rules = value.String
			}
		default:
			fp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FCirclePost.
// This includes values selected through modifiers, order, etc.
func (fp *FCirclePost) Value(name string) (ent.Value, error) {
	return fp.selectValues.Get(name)
}

// Update returns a builder for updating this FCirclePost.
// Note that you need to call FCirclePost.Unwrap() before calling this method if this FCirclePost
// was returned from a transaction, and the transaction was committed or rolled back.
func (fp *FCirclePost) Update() *FCirclePostUpdateOne {
	return NewFCirclePostClient(fp.config).UpdateOne(fp)
}

// Unwrap unwraps the FCirclePost entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (fp *FCirclePost) Unwrap() *FCirclePost {
	_tx, ok := fp.config.driver.(*txDriver)
	if !ok {
		panic("ent: FCirclePost is not a transactional entity")
	}
	fp.config.driver = _tx.drv
	return fp
}

// String implements the fmt.Stringer.
func (fp *FCirclePost) String() string {
	var builder strings.Builder
	builder.WriteString("FCirclePost(")
	builder.WriteString(fmt.Sprintf("id=%v, ", fp.ID))
	builder.WriteString("title=")
	builder.WriteString(fp.Title)
	builder.WriteString(", ")
	builder.WriteString("link=")
	builder.WriteString(fp.Link)
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(fp.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated=")
	builder.WriteString(fp.Updated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("author=")
	builder.WriteString(fp.Author)
	builder.WriteString(", ")
	builder.WriteString("avatar=")
	builder.WriteString(fp.Avatar)
	builder.WriteString(", ")
	builder.WriteString("friend_link=")
	builder.WriteString(fp.FriendLink)
	builder.WriteString(", ")
	builder.WriteString("crawled_at=")
	builder.WriteString(fp.CrawledAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("rules=")
	builder.WriteString(fp.Rules)
	builder.WriteByte(')')
	return builder.String()
}

// FCirclePosts is a parsable slice of FCirclePost.
type FCirclePosts []*FCirclePost
