// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/anzhiyu-c/anheyu-app/ent/givemoney"
	"github.com/anzhiyu-c/anheyu-app/ent/predicate"
)

// GiveMoneyQuery is the builder for querying GiveMoney entities.
type GiveMoneyQuery struct {
	config
	ctx        *QueryContext
	order      []givemoney.OrderOption
	inters     []Interceptor
	predicates []predicate.GiveMoney
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GiveMoneyQuery builder.
func (gmq *GiveMoneyQuery) Where(ps ...predicate.GiveMoney) *GiveMoneyQuery {
	gmq.predicates = append(gmq.predicates, ps...)
	return gmq
}

// Limit the number of records to be returned by this query.
func (gmq *GiveMoneyQuery) Limit(limit int) *GiveMoneyQuery {
	gmq.ctx.Limit = &limit
	return gmq
}

// Offset to start from.
func (gmq *GiveMoneyQuery) Offset(offset int) *GiveMoneyQuery {
	gmq.ctx.Offset = &offset
	return gmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gmq *GiveMoneyQuery) Unique(unique bool) *GiveMoneyQuery {
	gmq.ctx.Unique = &unique
	return gmq
}

// Order specifies how the records should be ordered.
func (gmq *GiveMoneyQuery) Order(o ...givemoney.OrderOption) *GiveMoneyQuery {
	gmq.order = append(gmq.order, o...)
	return gmq
}

// First returns the first GiveMoney entity from the query.
// Returns a *NotFoundError when no GiveMoney was found.
func (gmq *GiveMoneyQuery) First(ctx context.Context) (*GiveMoney, error) {
	nodes, err := gmq.Limit(1).All(setContextOp(ctx, gmq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{givemoney.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gmq *GiveMoneyQuery) FirstX(ctx context.Context) *GiveMoney {
	node, err := gmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GiveMoney ID from the query.
// Returns a *NotFoundError when no GiveMoney ID was found.
func (gmq *GiveMoneyQuery) FirstID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = gmq.Limit(1).IDs(setContextOp(ctx, gmq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{givemoney.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gmq *GiveMoneyQuery) FirstIDX(ctx context.Context) uint {
	id, err := gmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GiveMoney entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GiveMoney entity is found.
// Returns a *NotFoundError when no GiveMoney entities are found.
func (gmq *GiveMoneyQuery) Only(ctx context.Context) (*GiveMoney, error) {
	nodes, err := gmq.Limit(2).All(setContextOp(ctx, gmq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{givemoney.Label}
	default:
		return nil, &NotSingularError{givemoney.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gmq *GiveMoneyQuery) OnlyX(ctx context.Context) *GiveMoney {
	node, err := gmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GiveMoney ID in the query.
// Returns a *NotSingularError when more than one GiveMoney ID is found.
// Returns a *NotFoundError when no entities are found.
func (gmq *GiveMoneyQuery) OnlyID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = gmq.Limit(2).IDs(setContextOp(ctx, gmq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{givemoney.Label}
	default:
		err = &NotSingularError{givemoney.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gmq *GiveMoneyQuery) OnlyIDX(ctx context.Context) uint {
	id, err := gmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GiveMoneys.
func (gmq *GiveMoneyQuery) All(ctx context.Context) ([]*GiveMoney, error) {
	ctx = setContextOp(ctx, gmq.ctx, ent.OpQueryAll)
	if err := gmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GiveMoney, *GiveMoneyQuery]()
	return withInterceptors[[]*GiveMoney](ctx, gmq, qr, gmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gmq *GiveMoneyQuery) AllX(ctx context.Context) []*GiveMoney {
	nodes, err := gmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GiveMoney IDs.
func (gmq *GiveMoneyQuery) IDs(ctx context.Context) (ids []uint, err error) {
	if gmq.ctx.Unique == nil && gmq.path != nil {
		gmq.Unique(true)
	}
	ctx = setContextOp(ctx, gmq.ctx, ent.OpQueryIDs)
	if err = gmq.Select(givemoney.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gmq *GiveMoneyQuery) IDsX(ctx context.Context) []uint {
	ids, err := gmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gmq *GiveMoneyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gmq.ctx, ent.OpQueryCount)
	if err := gmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gmq, querierCount[*GiveMoneyQuery](), gmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gmq *GiveMoneyQuery) CountX(ctx context.Context) int {
	count, err := gmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gmq *GiveMoneyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gmq.ctx, ent.OpQueryExist)
	switch _, err := gmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gmq *GiveMoneyQuery) ExistX(ctx context.Context) bool {
	exist, err := gmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GiveMoneyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gmq *GiveMoneyQuery) Clone() *GiveMoneyQuery {
	if gmq == nil {
		return nil
	}
	return &GiveMoneyQuery{
		config:     gmq.config,
		ctx:        gmq.ctx.Clone(),
		order:      append([]givemoney.OrderOption{}, gmq.order...),
		inters:     append([]Interceptor{}, gmq.inters...),
		predicates: append([]predicate.GiveMoney{}, gmq.predicates...),
		// clone intermediate query.
		sql:       gmq.sql.Clone(),
		path:      gmq.path,
		modifiers: append([]func(*sql.Selector){}, gmq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GiveMoney.Query().
//		GroupBy(givemoney.FieldDeletedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (gmq *GiveMoneyQuery) GroupBy(field string, fields ...string) *GiveMoneyGroupBy {
	gmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GiveMoneyGroupBy{build: gmq}
	grbuild.flds = &gmq.ctx.Fields
	grbuild.label = givemoney.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.GiveMoney.Query().
//		Select(givemoney.FieldDeletedAt).
//		Scan(ctx, &v)
func (gmq *GiveMoneyQuery) Select(fields ...string) *GiveMoneySelect {
	gmq.ctx.Fields = append(gmq.ctx.Fields, fields...)
	sbuild := &GiveMoneySelect{GiveMoneyQuery: gmq}
	sbuild.label = givemoney.Label
	sbuild.flds, sbuild.scan = &gmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GiveMoneySelect configured with the given aggregations.
func (gmq *GiveMoneyQuery) Aggregate(fns ...AggregateFunc) *GiveMoneySelect {
	return gmq.Select().Aggregate(fns...)
}

func (gmq *GiveMoneyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gmq); err != nil {
				return err
			}
		}
	}
	for _, f := range gmq.ctx.Fields {
		if !givemoney.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gmq.path != nil {
		prev, err := gmq.path(ctx)
		if err != nil {
			return err
		}
		gmq.sql = prev
	}
	return nil
}

func (gmq *GiveMoneyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GiveMoney, error) {
	var (
		nodes = []*GiveMoney{}
		_spec = gmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GiveMoney).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GiveMoney{config: gmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(gmq.modifiers) > 0 {
		_spec.Modifiers = gmq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gmq *GiveMoneyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gmq.querySpec()
	if len(gmq.modifiers) > 0 {
		_spec.Modifiers = gmq.modifiers
	}
	_spec.Node.Columns = gmq.ctx.Fields
	if len(gmq.ctx.Fields) > 0 {
		_spec.Unique = gmq.ctx.Unique != nil && *gmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gmq.driver, _spec)
}

func (gmq *GiveMoneyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(givemoney.Table, givemoney.Columns, sqlgraph.NewFieldSpec(givemoney.FieldID, field.TypeUint))
	_spec.From = gmq.sql
	if unique := gmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gmq.path != nil {
		_spec.Unique = true
	}
	if fields := gmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, givemoney.FieldID)
		for i := range fields {
			if fields[i] != givemoney.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gmq *GiveMoneyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gmq.driver.Dialect())
	t1 := builder.Table(givemoney.Table)
	columns := gmq.ctx.Fields
	if len(columns) == 0 {
		columns = givemoney.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gmq.sql != nil {
		selector = gmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gmq.ctx.Unique != nil && *gmq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range gmq.modifiers {
		m(selector)
	}
	for _, p := range gmq.predicates {
		p(selector)
	}
	for _, p := range gmq.order {
		p(selector)
	}
	if offset := gmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gmq *GiveMoneyQuery) Modify(modifiers ...func(s *sql.Selector)) *GiveMoneySelect {
	gmq.modifiers = append(gmq.modifiers, modifiers...)
	return gmq.Select()
}

// GiveMoneyGroupBy is the group-by builder for GiveMoney entities.
type GiveMoneyGroupBy struct {
	selector
	build *GiveMoneyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gmgb *GiveMoneyGroupBy) Aggregate(fns ...AggregateFunc) *GiveMoneyGroupBy {
	gmgb.fns = append(gmgb.fns, fns...)
	return gmgb
}

// Scan applies the selector query and scans the result into the given value.
func (gmgb *GiveMoneyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gmgb.build.ctx, ent.OpQueryGroupBy)
	if err := gmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GiveMoneyQuery, *GiveMoneyGroupBy](ctx, gmgb.build, gmgb, gmgb.build.inters, v)
}

func (gmgb *GiveMoneyGroupBy) sqlScan(ctx context.Context, root *GiveMoneyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gmgb.fns))
	for _, fn := range gmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gmgb.flds)+len(gmgb.fns))
		for _, f := range *gmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GiveMoneySelect is the builder for selecting fields of GiveMoney entities.
type GiveMoneySelect struct {
	*GiveMoneyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gms *GiveMoneySelect) Aggregate(fns ...AggregateFunc) *GiveMoneySelect {
	gms.fns = append(gms.fns, fns...)
	return gms
}

// Scan applies the selector query and scans the result into the given value.
func (gms *GiveMoneySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gms.ctx, ent.OpQuerySelect)
	if err := gms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GiveMoneyQuery, *GiveMoneySelect](ctx, gms.GiveMoneyQuery, gms, gms.inters, v)
}

func (gms *GiveMoneySelect) sqlScan(ctx context.Context, root *GiveMoneyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gms.fns))
	for _, fn := range gms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gms *GiveMoneySelect) Modify(modifiers ...func(s *sql.Selector)) *GiveMoneySelect {
	gms.modifiers = append(gms.modifiers, modifiers...)
	return gms
}
