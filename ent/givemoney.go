// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/anzhiyu-c/anheyu-app/ent/givemoney"
)

// 打赏记录表
type GiveMoney struct {
	config `json:"-"`
	// ID of the ent.
	// 主键ID
	ID uint `json:"id,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 更新时间
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 昵称
	Nickname string `json:"nickname,omitempty"`
	// 金额
	Figure       int `json:"figure,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GiveMoney) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case givemoney.FieldID, givemoney.FieldFigure:
			values[i] = new(sql.NullInt64)
		case givemoney.FieldNickname:
			values[i] = new(sql.NullString)
		case givemoney.FieldDeletedAt, givemoney.FieldCreatedAt, givemoney.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GiveMoney fields.
func (gm *GiveMoney) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case givemoney.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			gm.ID = uint(value.Int64)
		case givemoney.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				gm.DeletedAt = new(time.Time)
				*gm.DeletedAt = value.Time
			}
		case givemoney.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				gm.CreatedAt = value.Time
			}
		case givemoney.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				gm.UpdatedAt = value.Time
			}
		case givemoney.FieldNickname:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field nickname", values[i])
			} else if value.Valid {
				gm.Nickname = value.String
			}
		case givemoney.FieldFigure:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field figure", values[i])
			} else if value.Valid {
				gm.Figure = int(value.Int64)
			}
		default:
			gm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GiveMoney.
// This includes values selected through modifiers, order, etc.
func (gm *GiveMoney) Value(name string) (ent.Value, error) {
	return gm.selectValues.Get(name)
}

// Update returns a builder for updating this GiveMoney.
// Note that you need to call GiveMoney.Unwrap() before calling this method if this GiveMoney
// was returned from a transaction, and the transaction was committed or rolled back.
func (gm *GiveMoney) Update() *GiveMoneyUpdateOne {
	return NewGiveMoneyClient(gm.config).UpdateOne(gm)
}

// Unwrap unwraps the GiveMoney entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gm *GiveMoney) Unwrap() *GiveMoney {
	_tx, ok := gm.config.driver.(*txDriver)
	if !ok {
		panic("ent: GiveMoney is not a transactional entity")
	}
	gm.config.driver = _tx.drv
	return gm
}

// String implements the fmt.Stringer.
func (gm *GiveMoney) String() string {
	var builder strings.Builder
	builder.WriteString("GiveMoney(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gm.ID))
	if v := gm.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(gm.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(gm.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("nickname=")
	builder.WriteString(gm.Nickname)
	builder.WriteString(", ")
	builder.WriteString("figure=")
	builder.WriteString(fmt.Sprintf("%v", gm.Figure))
	builder.WriteByte(')')
	return builder.String()
}

// GiveMoneys is a parsable slice of GiveMoney.
type GiveMoneys []*GiveMoney
