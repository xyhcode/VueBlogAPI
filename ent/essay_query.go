// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/anzhiyu-c/anheyu-app/ent/essay"
	"github.com/anzhiyu-c/anheyu-app/ent/predicate"
)

// EssayQuery is the builder for querying Essay entities.
type EssayQuery struct {
	config
	ctx        *QueryContext
	order      []essay.OrderOption
	inters     []Interceptor
	predicates []predicate.Essay
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EssayQuery builder.
func (eq *EssayQuery) Where(ps ...predicate.Essay) *EssayQuery {
	eq.predicates = append(eq.predicates, ps...)
	return eq
}

// Limit the number of records to be returned by this query.
func (eq *EssayQuery) Limit(limit int) *EssayQuery {
	eq.ctx.Limit = &limit
	return eq
}

// Offset to start from.
func (eq *EssayQuery) Offset(offset int) *EssayQuery {
	eq.ctx.Offset = &offset
	return eq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eq *EssayQuery) Unique(unique bool) *EssayQuery {
	eq.ctx.Unique = &unique
	return eq
}

// Order specifies how the records should be ordered.
func (eq *EssayQuery) Order(o ...essay.OrderOption) *EssayQuery {
	eq.order = append(eq.order, o...)
	return eq
}

// First returns the first Essay entity from the query.
// Returns a *NotFoundError when no Essay was found.
func (eq *EssayQuery) First(ctx context.Context) (*Essay, error) {
	nodes, err := eq.Limit(1).All(setContextOp(ctx, eq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{essay.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eq *EssayQuery) FirstX(ctx context.Context) *Essay {
	node, err := eq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Essay ID from the query.
// Returns a *NotFoundError when no Essay ID was found.
func (eq *EssayQuery) FirstID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = eq.Limit(1).IDs(setContextOp(ctx, eq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{essay.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eq *EssayQuery) FirstIDX(ctx context.Context) uint {
	id, err := eq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Essay entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Essay entity is found.
// Returns a *NotFoundError when no Essay entities are found.
func (eq *EssayQuery) Only(ctx context.Context) (*Essay, error) {
	nodes, err := eq.Limit(2).All(setContextOp(ctx, eq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{essay.Label}
	default:
		return nil, &NotSingularError{essay.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eq *EssayQuery) OnlyX(ctx context.Context) *Essay {
	node, err := eq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Essay ID in the query.
// Returns a *NotSingularError when more than one Essay ID is found.
// Returns a *NotFoundError when no entities are found.
func (eq *EssayQuery) OnlyID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = eq.Limit(2).IDs(setContextOp(ctx, eq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{essay.Label}
	default:
		err = &NotSingularError{essay.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eq *EssayQuery) OnlyIDX(ctx context.Context) uint {
	id, err := eq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Essays.
func (eq *EssayQuery) All(ctx context.Context) ([]*Essay, error) {
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryAll)
	if err := eq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Essay, *EssayQuery]()
	return withInterceptors[[]*Essay](ctx, eq, qr, eq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eq *EssayQuery) AllX(ctx context.Context) []*Essay {
	nodes, err := eq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Essay IDs.
func (eq *EssayQuery) IDs(ctx context.Context) (ids []uint, err error) {
	if eq.ctx.Unique == nil && eq.path != nil {
		eq.Unique(true)
	}
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryIDs)
	if err = eq.Select(essay.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eq *EssayQuery) IDsX(ctx context.Context) []uint {
	ids, err := eq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eq *EssayQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryCount)
	if err := eq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eq, querierCount[*EssayQuery](), eq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eq *EssayQuery) CountX(ctx context.Context) int {
	count, err := eq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eq *EssayQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryExist)
	switch _, err := eq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eq *EssayQuery) ExistX(ctx context.Context) bool {
	exist, err := eq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EssayQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eq *EssayQuery) Clone() *EssayQuery {
	if eq == nil {
		return nil
	}
	return &EssayQuery{
		config:     eq.config,
		ctx:        eq.ctx.Clone(),
		order:      append([]essay.OrderOption{}, eq.order...),
		inters:     append([]Interceptor{}, eq.inters...),
		predicates: append([]predicate.Essay{}, eq.predicates...),
		// clone intermediate query.
		sql:       eq.sql.Clone(),
		path:      eq.path,
		modifiers: append([]func(*sql.Selector){}, eq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Essay.Query().
//		GroupBy(essay.FieldDeletedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eq *EssayQuery) GroupBy(field string, fields ...string) *EssayGroupBy {
	eq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EssayGroupBy{build: eq}
	grbuild.flds = &eq.ctx.Fields
	grbuild.label = essay.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.Essay.Query().
//		Select(essay.FieldDeletedAt).
//		Scan(ctx, &v)
func (eq *EssayQuery) Select(fields ...string) *EssaySelect {
	eq.ctx.Fields = append(eq.ctx.Fields, fields...)
	sbuild := &EssaySelect{EssayQuery: eq}
	sbuild.label = essay.Label
	sbuild.flds, sbuild.scan = &eq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EssaySelect configured with the given aggregations.
func (eq *EssayQuery) Aggregate(fns ...AggregateFunc) *EssaySelect {
	return eq.Select().Aggregate(fns...)
}

func (eq *EssayQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eq); err != nil {
				return err
			}
		}
	}
	for _, f := range eq.ctx.Fields {
		if !essay.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eq.path != nil {
		prev, err := eq.path(ctx)
		if err != nil {
			return err
		}
		eq.sql = prev
	}
	return nil
}

func (eq *EssayQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Essay, error) {
	var (
		nodes = []*Essay{}
		_spec = eq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Essay).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Essay{config: eq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(eq.modifiers) > 0 {
		_spec.Modifiers = eq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eq *EssayQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eq.querySpec()
	if len(eq.modifiers) > 0 {
		_spec.Modifiers = eq.modifiers
	}
	_spec.Node.Columns = eq.ctx.Fields
	if len(eq.ctx.Fields) > 0 {
		_spec.Unique = eq.ctx.Unique != nil && *eq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eq.driver, _spec)
}

func (eq *EssayQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(essay.Table, essay.Columns, sqlgraph.NewFieldSpec(essay.FieldID, field.TypeUint))
	_spec.From = eq.sql
	if unique := eq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eq.path != nil {
		_spec.Unique = true
	}
	if fields := eq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, essay.FieldID)
		for i := range fields {
			if fields[i] != essay.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eq *EssayQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eq.driver.Dialect())
	t1 := builder.Table(essay.Table)
	columns := eq.ctx.Fields
	if len(columns) == 0 {
		columns = essay.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eq.sql != nil {
		selector = eq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eq.ctx.Unique != nil && *eq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range eq.modifiers {
		m(selector)
	}
	for _, p := range eq.predicates {
		p(selector)
	}
	for _, p := range eq.order {
		p(selector)
	}
	if offset := eq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eq *EssayQuery) Modify(modifiers ...func(s *sql.Selector)) *EssaySelect {
	eq.modifiers = append(eq.modifiers, modifiers...)
	return eq.Select()
}

// EssayGroupBy is the group-by builder for Essay entities.
type EssayGroupBy struct {
	selector
	build *EssayQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (egb *EssayGroupBy) Aggregate(fns ...AggregateFunc) *EssayGroupBy {
	egb.fns = append(egb.fns, fns...)
	return egb
}

// Scan applies the selector query and scans the result into the given value.
func (egb *EssayGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, egb.build.ctx, ent.OpQueryGroupBy)
	if err := egb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EssayQuery, *EssayGroupBy](ctx, egb.build, egb, egb.build.inters, v)
}

func (egb *EssayGroupBy) sqlScan(ctx context.Context, root *EssayQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(egb.fns))
	for _, fn := range egb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*egb.flds)+len(egb.fns))
		for _, f := range *egb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*egb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := egb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EssaySelect is the builder for selecting fields of Essay entities.
type EssaySelect struct {
	*EssayQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (es *EssaySelect) Aggregate(fns ...AggregateFunc) *EssaySelect {
	es.fns = append(es.fns, fns...)
	return es
}

// Scan applies the selector query and scans the result into the given value.
func (es *EssaySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, es.ctx, ent.OpQuerySelect)
	if err := es.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EssayQuery, *EssaySelect](ctx, es.EssayQuery, es, es.inters, v)
}

func (es *EssaySelect) sqlScan(ctx context.Context, root *EssayQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(es.fns))
	for _, fn := range es.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*es.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := es.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (es *EssaySelect) Modify(modifiers ...func(s *sql.Selector)) *EssaySelect {
	es.modifiers = append(es.modifiers, modifiers...)
	return es
}
